// import { actionTypes } from '../util/constants';

// export const setOption = (key, value) => Promise.resolve({
//     type: actionTypes.jPlaylist.SET_OPTION,
//     key: key,
//     value: value
// });
// export const addUniqueToArray = generator(actionTypes.jPlaylist.ARRAY_ADD_UNIQUE, "key", "value");
// export const removeFromArrayByValue = generator(actionTypes.jPlaylist.ARRAY_REMOVE_BY_VALUE, "key", "value");
// export const removeFromArrayByIndex = generator(actionTypes.jPlaylist.ARRAY_REMOVE_BY_INDEX, "key", "value");

/*
onShuffleClick = (event) => {
    event.preventDefault();

    this.context.shuffle(!this.props.shuffled);
    this.context.blur(event.target);
}
onPreviousClick = (event) => {
    event.preventDefault();

    this.context.previous();
    this.context.blur(event.target);
}
onNextClick = (event) => {
    event.preventDefault();

    this.context.next();
    this.context.blur(event.target);
}
onVideoPlayClick = () => this.props.dispatch(play())
shuffle: (<a className={classes.SHUFFLE} onClick={props.onShuffleClick}>{props.children}</a>),
previous: (<a className={classes.PREVIOUS} onClick={props.onPreviousClick}>{props.children}</a>),
next: (<a className={classes.NEXT} onClick={props.onNextClick}>{props.children}</a>);
*/